import { Button } from "@mui/material"

// #author - 왕인서3
// #title - common
// #prop - content:버튼 컨텐츠, variant: 테마2, color:색깔
const TempButton = (prop) => {
  const { content, variant } = prop;
  return (
    <Button>hihi</Button>
  )
}

export default TempButton;